export enum Enviroments {
	LOCAL = 'local',
	PRE = 'pre',
	PRO = 'production',
}

export enum Storages {
	LOCAL = 'localStorage',
	SESSION = 'sessionStorage',
}

export enum StorageKeys {
	USER = 'user',
}

export enum Routes {
	HOME = '/',
}
